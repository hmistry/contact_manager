= simple_form_for(@phone_number) do |f|
  = f.error_notification

  .form-group
    = f.input :number, input_html: {class: 'form-control'}
  .form-group
    = f.input :person_id, input_html: {class: 'form-control'}

  .form-group
    = f.button :submit, class: 'btn-primary'
